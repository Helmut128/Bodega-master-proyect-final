export interface Mesas {
    mesas: any;
    ancho: any; // Cambiado a number
    alto: any; // Cambiado a number
    position?: { x: number;y: number };
    rotation: boolean; // Nueva propiedad para almacenar la rotación
    location?: { x: number; y: number };
 
}
